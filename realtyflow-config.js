/* Optional production configuration.
   Set these values after the Realtyflow deployment URL and source key are ready.
*/
window.REALTYFLOW_BOOKING_API_URL = window.REALTYFLOW_BOOKING_API_URL || "https://realtyflow.chatgenius.pro/api/public/booking-leads";
window.REALTYFLOW_BOOKING_SOURCE_KEY = window.REALTYFLOW_BOOKING_SOURCE_KEY || "";
